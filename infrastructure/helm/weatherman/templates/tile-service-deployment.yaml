apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-tile-service
  labels:
    {{- include "weatherman.labels" . | nindent 4 }}
    app.kubernetes.io/component: tile-service
spec:
  replicas: {{ .Values.tileService.replicaCount }}
  selector:
    matchLabels:
      {{- include "weatherman.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "tile-service") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "weatherman.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "tile-service") | nindent 8 }}
    spec:
      containers:
        - name: tile-service
          image: "{{ .Values.tileService.image.repository | default .Values.global.image.repository }}:{{ .Values.tileService.image.tag | default .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.tileService.port }}
              protocol: TCP
          livenessProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.tileService.probes.liveness) | nindent 12 }}
          readinessProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.tileService.probes.readiness) | nindent 12 }}
          startupProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.tileService.probes.startup) | nindent 12 }}
          resources:
            {{- toYaml .Values.tileService.resources | nindent 12 }}
