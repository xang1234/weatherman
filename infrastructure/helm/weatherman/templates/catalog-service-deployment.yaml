apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-catalog-service
  labels:
    {{- include "weatherman.labels" . | nindent 4 }}
    app.kubernetes.io/component: catalog-service
spec:
  replicas: {{ .Values.catalogService.replicaCount }}
  selector:
    matchLabels:
      {{- include "weatherman.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "catalog-service") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "weatherman.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "catalog-service") | nindent 8 }}
    spec:
      containers:
        - name: catalog-service
          image: "{{ .Values.catalogService.image.repository | default .Values.global.image.repository }}:{{ .Values.catalogService.image.tag | default .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          {{- $env := mergeOverwrite (dict) (.Values.global.env | default dict) (.Values.catalogService.env | default dict) }}
          {{- $secretEnv := mergeOverwrite (dict) (.Values.global.secretEnv | default dict) (.Values.catalogService.secretEnv | default dict) }}
          {{- if or (gt (len $env) 0) (gt (len $secretEnv) 0) }}
          env:
            {{- include "weatherman.containerEnv" (dict "globalEnv" .Values.global.env "componentEnv" .Values.catalogService.env "globalSecretEnv" .Values.global.secretEnv "componentSecretEnv" .Values.catalogService.secretEnv) | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.catalogService.port }}
              protocol: TCP
          livenessProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.catalogService.probes.liveness) | nindent 12 }}
          readinessProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.catalogService.probes.readiness) | nindent 12 }}
          startupProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.catalogService.probes.startup) | nindent 12 }}
          resources:
            {{- toYaml .Values.catalogService.resources | nindent 12 }}
