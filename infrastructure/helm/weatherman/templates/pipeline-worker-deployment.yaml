apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-pipeline-worker
  labels:
    {{- include "weatherman.labels" . | nindent 4 }}
    app.kubernetes.io/component: pipeline-worker
spec:
  replicas: {{ .Values.pipelineWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "weatherman.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "pipeline-worker") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "weatherman.selectorLabels" (dict "Chart" .Chart "Release" .Release "component" "pipeline-worker") | nindent 8 }}
    spec:
      containers:
        - name: pipeline-worker
          image: "{{ .Values.pipelineWorker.image.repository | default .Values.global.image.repository }}:{{ .Values.pipelineWorker.image.tag | default .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          {{- $env := mergeOverwrite (dict) (.Values.global.env | default dict) (.Values.pipelineWorker.env | default dict) }}
          {{- $secretEnv := mergeOverwrite (dict) (.Values.global.secretEnv | default dict) (.Values.pipelineWorker.secretEnv | default dict) }}
          {{- if or (gt (len $env) 0) (gt (len $secretEnv) 0) }}
          env:
            {{- include "weatherman.containerEnv" (dict "globalEnv" .Values.global.env "componentEnv" .Values.pipelineWorker.env "globalSecretEnv" .Values.global.secretEnv "componentSecretEnv" .Values.pipelineWorker.secretEnv) | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.pipelineWorker.port }}
              protocol: TCP
          livenessProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.pipelineWorker.probes.liveness) | nindent 12 }}
          readinessProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.pipelineWorker.probes.readiness) | nindent 12 }}
          startupProbe:
            {{- include "weatherman.probe" (dict "probe" .Values.pipelineWorker.probes.startup) | nindent 12 }}
          resources:
            {{- toYaml .Values.pipelineWorker.resources | nindent 12 }}
